mailman headless-keystore verification
package: @indianic/mailman@1.2.1
date:    2026-08-03T08:34:11Z
host:    Darwin arm64 · docker linux/arm64
image:   ubuntu:24.04 + node 20 (musl mode uses node:20-alpine) · platform linux/amd64 (emulated)
modes:   none libsecret full musl
=======================================================================
=======================================================================
mailman headless-keystore checks · KEYRING_LEVEL=none
package: @indianic/mailman@1.2.1
node:    v20.20.2   arch: x86_64
=======================================================================

── environment is what we think it is ──
  PASS  libsecret is absent from the library path
  PASS  keytar itself cannot load (libsecret linked at runtime)

── the CLI starts at all ──
  PASS  mailman --version works (1.2.1)
  PASS  mailman help works

── doctor tells the truth about this machine ──
      | ■  libsecret: NOT FOUND — keytar links it at runtime, so its native module cannot even load without it
      | ■  OS credential store: unreachable — keytar's native module could not load at all, so this is a missing or unusable libsecret rather than a missing daemon: libsecret-1.so.0: cannot open shared object file: No such file or directory
      | ◇  Keystore backend: os-keychain, via the Linux Secret Service: the key is machine-bound and never touches the config dir [default (no keystore.json yet)]
      | ◇  Scheduled-send ticker: not installed yet (would use crontab)
      | │  missing: libsecret — run: mailman doctor --fix
  PASS  doctor runs instead of crashing
  PASS  libsecret reported NOT FOUND
  PASS  credential store reported unreachable
  PASS  no false 'keytar can reach the Secret Service' claim
  PASS  --fix no longer says 'log into a desktop session' first
  PASS  --fix points at a headless keystore

── passphrase keystore: full credential round trip ──
  PASS  configure + decrypt an account with no keyring at all
  PASS  accounts.json was written
  PASS  the app password is NOT on disk in plaintext
  PASS  the passphrase is NOT on disk
  PASS  keystore.json records the backend
  PASS  keystore.json holds a salt, not a key
  PASS  a wrong passphrase is refused in a fresh process
  PASS  a wrong passphrase does not decrypt anything
  PASS  ...and is refused with an explanation, not a crypto error
  PASS  doctor names the active backend
  PASS  unreachable credential store is not counted against a passphrase setup
  PASS  libsecret is not a missing dependency for this keystore
  PASS  ...so nothing is reported as missing
  PASS  ...and the whole run is green

── env keystore: the container/CI shape ──
  PASS  configure + decrypt from MAILMAN_MASTER_KEY
  PASS  the key itself is never persisted
  PASS  a truncated MAILMAN_MASTER_KEY is refused by length

── file keystore: key kept out of the config dir ──
  PASS  configure + decrypt via a key file
  PASS  the create is loudly labelled
  PASS  the key file is 0600
  PASS  the key file lives outside the config dir
  PASS  doctor reports the file backend as degraded, not healthy
  PASS  reset deletes the out-of-tree key file

── never orphan an existing key ──
  PASS  switching keystores over existing ciphertext is refused
  PASS  ...and no second key was minted
  PASS  ...with migrate-keystore named as the way to do it properly

── rotate-key covers scheduled sends too ──
  PASS  rotation completes
  PASS  a queued scheduled send is still readable after rotation

── the cron ticker carries its own environment ──
  PASS  ticker installed into crontab
  PASS  cron line carries DBUS_SESSION_BUS_ADDRESS
  PASS  cron line carries XDG_RUNTIME_DIR
  PASS  cron line pins the keystore
  PASS  cron line carries the config dir
  PASS  cron line still sets PATH first
  PASS  cron line does NOT contain the passphrase
  PASS  cron line does not leak a passphrase var at all
  PASS  (precondition) the line is now stale
  PASS  a stale ticker line is repaired on the next install
  PASS  send-scheduled --due runs with no session bus
  PASS  ...and not by autolaunching D-Bus

=======================================================================
RESULT: PASS — 50 checks, 0 failures (KEYRING_LEVEL=none)
=======================================================================

=======================================================================
mailman headless-keystore checks · KEYRING_LEVEL=libsecret
package: @indianic/mailman@1.2.1
node:    v20.20.2   arch: x86_64
=======================================================================

── environment is what we think it is ──
  PASS  libsecret IS on the library path
  PASS  keytar loads fine — only the daemon is missing

── the CLI starts at all ──
  PASS  mailman --version works (1.2.1)
  PASS  mailman help works

── doctor tells the truth about this machine ──
      | ◇  libsecret: present (libsecret-1.so is on the library path) — the library only; see `OS credential store` below
      | ■  OS credential store: unreachable — the library is there, but nothing is serving the Secret Service on the session bus (no gnome-keyring/kwallet): Cannot autolaunch D-Bus without X11 $DISPLAY
      | ◇  Keystore backend: os-keychain, via the Linux Secret Service: the key is machine-bound and never touches the config dir [default (no keystore.json yet)]
      | ◇  Scheduled-send ticker: not installed yet (would use crontab)
  PASS  libsecret reported present, library only
  PASS  credential store reported unreachable
  PASS  diagnosed as a missing DAEMON, not a missing library
  PASS  no false 'keytar can reach the Secret Service' claim
  PASS  --fix no longer says 'log into a desktop session' first
  PASS  --fix points at a headless keystore

── passphrase keystore: full credential round trip ──
  PASS  configure + decrypt an account with no keyring at all
  PASS  accounts.json was written
  PASS  the app password is NOT on disk in plaintext
  PASS  the passphrase is NOT on disk
  PASS  keystore.json records the backend
  PASS  keystore.json holds a salt, not a key
  PASS  a wrong passphrase is refused in a fresh process
  PASS  a wrong passphrase does not decrypt anything
  PASS  ...and is refused with an explanation, not a crypto error
  PASS  doctor names the active backend
  PASS  unreachable credential store is not counted against a passphrase setup
  PASS  libsecret is not a missing dependency for this keystore
  PASS  ...so nothing is reported as missing
  PASS  ...and the whole run is green

── env keystore: the container/CI shape ──
  PASS  configure + decrypt from MAILMAN_MASTER_KEY
  PASS  the key itself is never persisted
  PASS  a truncated MAILMAN_MASTER_KEY is refused by length

── file keystore: key kept out of the config dir ──
  PASS  configure + decrypt via a key file
  PASS  the create is loudly labelled
  PASS  the key file is 0600
  PASS  the key file lives outside the config dir
  PASS  doctor reports the file backend as degraded, not healthy
  PASS  reset deletes the out-of-tree key file

── never orphan an existing key ──
  PASS  switching keystores over existing ciphertext is refused
  PASS  ...and no second key was minted
  PASS  ...with migrate-keystore named as the way to do it properly

── rotate-key covers scheduled sends too ──
  PASS  rotation completes
  PASS  a queued scheduled send is still readable after rotation

── the cron ticker carries its own environment ──
  PASS  ticker installed into crontab
  PASS  cron line carries DBUS_SESSION_BUS_ADDRESS
  PASS  cron line carries XDG_RUNTIME_DIR
  PASS  cron line pins the keystore
  PASS  cron line carries the config dir
  PASS  cron line still sets PATH first
  PASS  cron line does NOT contain the passphrase
  PASS  cron line does not leak a passphrase var at all
  PASS  (precondition) the line is now stale
  PASS  a stale ticker line is repaired on the next install
  PASS  send-scheduled --due runs with no session bus
  PASS  ...and not by autolaunching D-Bus

=======================================================================
RESULT: PASS — 50 checks, 0 failures (KEYRING_LEVEL=libsecret)
=======================================================================

=======================================================================
mailman headless-keystore checks · KEYRING_LEVEL=full
package: @indianic/mailman@1.2.1
node:    v20.20.2   arch: x86_64
=======================================================================

── environment is what we think it is ──
  PASS  libsecret IS on the library path
  PASS  keytar loads fine

── the CLI starts at all ──
  PASS  mailman --version works (1.2.1)
  PASS  mailman help works

── doctor tells the truth about this machine ──
      | ◇  libsecret: present (libsecret-1.so is on the library path) — the library only; see `OS credential store` below
      | ◇  OS credential store: reachable (write/read/delete probe succeeded)
      | ◇  Keystore backend: os-keychain, via the Linux Secret Service: the key is machine-bound and never touches the config dir [default (no keystore.json yet)]
      | ◇  Scheduled-send ticker: not installed yet (would use crontab)
  PASS  credential store reachable
  PASS  keystore backend is os-keychain by default

── passphrase keystore: full credential round trip ──
  PASS  configure + decrypt an account with no keyring at all
  PASS  accounts.json was written
  PASS  the app password is NOT on disk in plaintext
  PASS  the passphrase is NOT on disk
  PASS  keystore.json records the backend
  PASS  keystore.json holds a salt, not a key
  PASS  a wrong passphrase is refused in a fresh process
  PASS  a wrong passphrase does not decrypt anything
  PASS  ...and is refused with an explanation, not a crypto error
  PASS  doctor names the active backend

── env keystore: the container/CI shape ──
  PASS  configure + decrypt from MAILMAN_MASTER_KEY
  PASS  the key itself is never persisted
  PASS  a truncated MAILMAN_MASTER_KEY is refused by length

── file keystore: key kept out of the config dir ──
  PASS  configure + decrypt via a key file
  PASS  the create is loudly labelled
  PASS  the key file is 0600
  PASS  the key file lives outside the config dir
  PASS  doctor reports the file backend as degraded, not healthy
  PASS  reset deletes the out-of-tree key file

── never orphan an existing key ──
  PASS  switching keystores over existing ciphertext is refused
  PASS  ...and no second key was minted
  PASS  ...with migrate-keystore named as the way to do it properly

── rotate-key covers scheduled sends too ──
  PASS  rotation completes
  PASS  a queued scheduled send is still readable after rotation

── the cron ticker carries its own environment ──
  PASS  ticker installed into crontab
  PASS  cron line carries DBUS_SESSION_BUS_ADDRESS
  PASS  cron line carries XDG_RUNTIME_DIR
  PASS  cron line pins the keystore
  PASS  cron line carries the config dir
  PASS  cron line still sets PATH first
  PASS  cron line does NOT contain the passphrase
  PASS  cron line does not leak a passphrase var at all
  PASS  (precondition) the line is now stale
  PASS  a stale ticker line is repaired on the next install
  PASS  send-scheduled --due runs with no session bus
  PASS  ...and not by autolaunching D-Bus

── os-keychain default path is unchanged ──
      | DECRYPTED:aaaa bbbb cccc dddd
      | BACKEND:os-keychain
  PASS  no MAILMAN_KEYSTORE set: the OS credential store is still used
  PASS  configure + decrypt on the default path
  PASS  the master key is in the OS credential store

── migrate-keystore moves a real key ──
  PASS  os-keychain -> passphrase re-encrypts
  PASS  ...pointer updated
  PASS  ...credentials still readable
  PASS  passphrase -> os-keychain moves the key without re-encrypting
  PASS  ...pointer updated
  PASS  ...credentials survive the round trip

=======================================================================
RESULT: PASS — 51 checks, 0 failures (KEYRING_LEVEL=full)
=======================================================================

=======================================================================
mailman headless-keystore checks · KEYRING_LEVEL=musl
package: @indianic/mailman@1.2.1
node:    v20.20.2   arch: x86_64
=======================================================================

── environment is what we think it is ──
  PASS  libsecret is absent from the library path
  PASS  keytar itself cannot load (libsecret linked at runtime)

── the CLI starts at all ──
  PASS  mailman --version works (1.2.1)
  PASS  mailman help works

── doctor tells the truth about this machine ──
      | ■  libsecret: NOT FOUND — keytar links it at runtime, so its native module cannot even load without it
      | ■  OS credential store: unreachable — keytar's native module could not load at all, so this is a missing or unusable libsecret rather than a missing daemon: Error loading shared library libsecret-1.so.0: No such file or directory (needed by /usr/local/lib/node_modules/@indianic/mailman/node_modules/keytar/build/Release/keytar.node)
      | ◇  Keystore backend: os-keychain, via the Linux Secret Service: the key is machine-bound and never touches the config dir [default (no keystore.json yet)]
      | ◇  Scheduled-send ticker: not installed yet (would use crontab)
      | │  missing: libsecret — run: mailman doctor --fix
  PASS  doctor runs instead of crashing
  PASS  libsecret probe is honest about the library
  PASS  credential store reported unreachable
  PASS  no false 'keytar can reach the Secret Service' claim
  PASS  --fix no longer says 'log into a desktop session' first
  PASS  --fix points at a headless keystore

── passphrase keystore: full credential round trip ──
  PASS  configure + decrypt an account with no keyring at all
  PASS  accounts.json was written
  PASS  the app password is NOT on disk in plaintext
  PASS  the passphrase is NOT on disk
  PASS  keystore.json records the backend
  PASS  keystore.json holds a salt, not a key
  PASS  a wrong passphrase is refused in a fresh process
  PASS  a wrong passphrase does not decrypt anything
  PASS  ...and is refused with an explanation, not a crypto error
  PASS  doctor names the active backend
  PASS  unreachable credential store is not counted against a passphrase setup
  PASS  libsecret is not a missing dependency for this keystore
  PASS  ...so nothing is reported as missing
  PASS  ...and the whole run is green

── env keystore: the container/CI shape ──
  PASS  configure + decrypt from MAILMAN_MASTER_KEY
  PASS  the key itself is never persisted
  PASS  a truncated MAILMAN_MASTER_KEY is refused by length

── file keystore: key kept out of the config dir ──
  PASS  configure + decrypt via a key file
  PASS  the create is loudly labelled
  PASS  the key file is 0600
  PASS  the key file lives outside the config dir
  PASS  doctor reports the file backend as degraded, not healthy
  PASS  reset deletes the out-of-tree key file

── never orphan an existing key ──
  PASS  switching keystores over existing ciphertext is refused
  PASS  ...and no second key was minted
  PASS  ...with migrate-keystore named as the way to do it properly

── rotate-key covers scheduled sends too ──
  PASS  rotation completes
  PASS  a queued scheduled send is still readable after rotation

── the cron ticker carries its own environment ──
  PASS  ticker installed into crontab
  PASS  cron line carries DBUS_SESSION_BUS_ADDRESS
  PASS  cron line carries XDG_RUNTIME_DIR
  PASS  cron line pins the keystore
  PASS  cron line carries the config dir
  PASS  cron line still sets PATH first
  PASS  cron line does NOT contain the passphrase
  PASS  cron line does not leak a passphrase var at all
  PASS  (precondition) the line is now stale
  PASS  a stale ticker line is repaired on the next install
  PASS  send-scheduled --due runs with no session bus
  PASS  ...and not by autolaunching D-Bus

=======================================================================
RESULT: PASS — 50 checks, 0 failures (KEYRING_LEVEL=musl)
=======================================================================

=======================================================================
RESULT: PASS — every mode green on Ubuntu 24.04 (node 20, linux/amd64)
