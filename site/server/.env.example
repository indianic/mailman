# Copy to .env and adjust as needed.
# The server always binds to this HOST:PORT — fixed, never random.
HOST=localhost
PORT=4000

# Local Postgres — user "kalpesh", empty password.
PGHOST=localhost
PGPORT=5432
PGUSER=kalpesh
PGPASSWORD=
PGDATABASE=mailman_site
